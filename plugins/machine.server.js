export default async ({
    store
}) => {
    // await store.dispatch('machineManagement/fetchRentMachines', { filters: { 'onboard_for': "RENT" } })
    // await store.dispatch('machineManagement/fetchBuyMachines', { filters: { 'onboard_for': "SELL" } })
    // await store.dispatch("machineManagement/fetchCategories", { limit: 5 });
    // await store.dispatch("machineManagement/fetchAddress", { limit: 5 });
}
